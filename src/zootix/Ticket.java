package zootix;

//Ticket interface that otherwise complies with terms of assignment for Ticket class
public interface Ticket {
	String getType();
	double getPrice();
	void setPrice(double price);
}
